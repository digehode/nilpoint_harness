 - Have a standard /nilpoint/game-slug set of URLs
 - Give each game a "slug" field to use as a redirect?
 - Have a pattern of "slug-x" for things like "cypherpunk-overview" so
   there's a neat way to get to main pages of each game?
